# Android fstab file.
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK

#<src>                                  <mnt_point>  <type>  <mnt_flags and options>                   <fs_mgr_flags>
/dev/block/bootdevice/by-name/system    /system      f2fs    discard,active_logs=2,background_gc=off   wait,check
/dev/block/bootdevice/by-name/userdata  /data        f2fs    discard,active_logs=2                     wait,check,encryptable=footer,resize